package com.ledgerx.command;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = com.ledgerx.command.CommandServiceApplication.class)
class CommandServiceApplicationTests {

    @Test
    void contextLoads() {
        // boots the Spring context
    }
}